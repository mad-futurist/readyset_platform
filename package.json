{
  "name": "readyset-platform",
  "private": true,
  "workspaces": ["apps/web", "packages/api-client"],
  "scripts": {
    "build": "npm run build --workspace @readyset/web",
    "export:openapi": "python apps/api/scripts/export_openapi.py",
    "lint": "npm run lint --workspace @readyset/web",
    "typecheck": "npm run typecheck --workspace @readyset/web",
    "test": "npm run test --workspace @readyset/web",
    "generate:api-client": "npm run generate --workspace @readyset/api-client"
  }
}
